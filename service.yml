apiVersion: v1
kind: Service
metadata:
  name: todo-app
#  namespace: prod
  labels:
    name: todo-app
spec:
  type: LoadBalancer
  selector:
    app: todo-app
  ports:
    - protocol: TCP
      name: http
      port: 80
      targetPort: 3000